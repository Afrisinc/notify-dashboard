# Base URL of the API Gateway, including the /notify prefix.
# The dashboard must never call notification-service directly - all
# requests go through the gateway, which authenticates the request and
# enforces role-based access before proxying to notification-service.
VITE_API_BASE_URL=http://localhost:8091/notify
VITE_AUTH_UI_URL=http://localhost:8098
